import { SelectionNode, DocumentNode, OperationDefinitionNode, FragmentSpreadNode, InlineFragmentNode } from 'graphql';
import { Fragments, Variables } from '../types';
/** Returns the main operation's definition */
export declare const getMainOperation: (doc: DocumentNode) => OperationDefinitionNode;
/** Returns a mapping from fragment names to their selections */
export declare const getFragments: (doc: DocumentNode) => Fragments;
/** Resolves @include and @skip directives to determine whether field is included. */
export declare const shouldInclude: (node: SelectionNode, vars: Variables) => boolean;
/** Resolves @defer directive to determine whether a fragment is potentially skipped. */
export declare const isDeferred: (node: FragmentSpreadNode | InlineFragmentNode, vars: Variables) => boolean;
